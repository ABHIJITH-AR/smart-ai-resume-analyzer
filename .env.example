# Port for the backend Express server (defaults to 3000 if not specified)
PORT=3000

# Secret key for JWT token signatures to secure user sessions and passwords
JWT_SECRET=your_jwt_secret_key_here

# Mode for Express and Vite (e.g., development or production)
NODE_ENV=development
